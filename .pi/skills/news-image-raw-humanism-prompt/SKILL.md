---
name: news-image-raw-humanism-prompt
description: Compile factual news into a raw-humanist editorial illustration prompt.
---

# Raw humanism news image

Compile only. Do not generate images, invoke Tools, read files, access the network or write artifacts. Return only the JSON requested by the Agent.

Treat the title and summary as the complete factual source. Preserve only supported facts. Freeze one Scene Kernel: one human body fragment performs one simple physical action on one or two abstract warm-white entities. Never depict the literal product, vehicle, named structure or full news scene. Use lifting, holding, pushing, stopping, aligning, building, passing through or guiding.

Use one flat official background color: Clay `#D97757`, Sky `#6A98BC`, Fig `#C46686`, Pale Sage `#BCD1CA`, Olive `#788C5D`, Oat `#E3DACC`, Heather `#CBCADB`, or Pale Coral `#EBCECE`. Draw one body fragment with dry uneven black `#141413`; add one or two simple solid warm-white `#FAF9F5` paper shapes. The compact gesture occupies 30–40%; at least 60% remains empty. Use two to five visual units, off-center and asymmetric.

Compile a 300–8,000 character English prompt with these headings in order: `Use case: stylized-concept`, `Asset type: horizontal editorial main visual, composed safely for a 4:3 crop`, `NEWS RELATION:`, `ONE SCENE KERNEL:`, `COMPOSITION:`, `PHYSICAL MAKING RECIPE:`, `TEXT:`, `NEGATIVE CONSTRAINTS:`.

The recipe must screen-print one perfectly flat official color edge to edge, draw one body fragment in black `#141413`, cut one or two simple warm-white `#FAF9F5` shapes, preserve at least 60% empty field, and stop immediately. Explicitly forbid gradients, vignettes, shadows, lighting, texture, internal detail, typography and extra icons.

The `TEXT` section must contain exactly: `No text, letters, numbers, logos or pseudo-text anywhere in the image.` Reject literal products or vehicles, complete landmarks or scenes, paired people, icon lists, multiple metaphors, logos, white ring outlines, isometric geometry, vector-logo polish, gradients, shadows, paper texture, 3D clay, text, watermarks and HUDs.

Return exactly `newsIdentity`, `coreTension`, `realityAnchor`, `factExclusions`, `sceneKernel`, and `prompt` as JSON.
